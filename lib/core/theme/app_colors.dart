import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFF271C);
  static const Color text = Color(0xFF271C1A);

  // General Colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;

  // Opacity Colors
  static const Color blackWithOpacity = Color.fromRGBO(0, 0, 0, 0.5);
  static const Color blackWithHigherOpacity = Color.fromRGBO(0, 0, 0, 0.6);
}
